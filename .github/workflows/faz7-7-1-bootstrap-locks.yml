name: faz7-7-1-bootstrap-locks

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  bootstrap-locks:
    runs-on: ubuntu-24.04
    env:
      SOURCE_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
      PYTHON_IMAGE_TAG: python:3.11.16-slim-bookworm
      DEBIAN_SNAPSHOT: '20260821T000000Z'
      SYFT_TAG: anchore/syft:v1.44.0
      GRYPE_TAG: anchore/grype:v0.116.1
      COSIGN_TAG: ghcr.io/sigstore/cosign/cosign:v3.1.2
      N8N_IMAGE: n8nio/n8n@sha256:f9a15cc65378e4e5b6c3b1445c83985131938db8d8b5b1ab891d7d50196b2162
    steps:
      - name: Checkout exact source head without third-party action
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git fetch --depth=1 origin "${SOURCE_SHA}"
          git checkout --detach FETCH_HEAD
          test "$(git rev-parse HEAD)" = "${SOURCE_SHA}"

      - name: Preserve committed lock authority
        run: |
          set -euo pipefail
          mkdir -p /tmp/committed-locks deploy/containers/artifacts
          cp deploy/containers/base-image.lock /tmp/committed-locks/base-image.lock
          cp deploy/containers/supply-chain-tools.lock /tmp/committed-locks/supply-chain-tools.lock
          cp deploy/containers/build-tools.lock /tmp/committed-locks/build-tools.lock
          cp deploy/containers/test-tools.lock /tmp/committed-locks/test-tools.lock
          cp deploy/containers/api/requirements.lock /tmp/committed-locks/api-requirements.lock
          cp deploy/containers/api/apt-runtime.lock /tmp/committed-locks/api-apt-runtime.lock
          cp deploy/containers/commerce/requirements.lock /tmp/committed-locks/commerce-requirements.lock

      - name: Re-resolve immutable container tool identities
        run: |
          set -euo pipefail
          mkdir -p /tmp/faz7-digests
          resolve_digest() {
            local image key output digest
            image="$1"; key="$2"; output="/tmp/faz7-digests/${key}.txt"
            docker buildx imagetools inspect "$image" > "$output"
            digest="$(awk '/^Digest:/ {print $2; found=1} found {exit}' "$output")"
            test -n "$digest"
            printf '%s' "$digest"
          }
          python_digest="$(resolve_digest "${PYTHON_IMAGE_TAG}" python)"
          syft_digest="$(resolve_digest "${SYFT_TAG}" syft)"
          grype_digest="$(resolve_digest "${GRYPE_TAG}" grype)"
          cosign_digest="$(resolve_digest "${COSIGN_TAG}" cosign)"
          for d in "$python_digest" "$syft_digest" "$grype_digest" "$cosign_digest"; do
            case "$d" in sha256:*) ;; *) echo "invalid digest: $d" >&2; exit 1;; esac
          done
          cat > deploy/containers/base-image.lock <<EOF
          # FAZ 7.1 immutable base-image authority
          PYTHON_IMAGE_TAG=${PYTHON_IMAGE_TAG}
          PYTHON_IMAGE_DIGEST=${python_digest}
          PYTHON_IMAGE=${PYTHON_IMAGE_TAG}@${python_digest}
          TARGET_PLATFORM=linux/amd64
          DEBIAN_SNAPSHOT=${DEBIAN_SNAPSHOT}
          EOF
          cat > deploy/containers/supply-chain-tools.lock <<EOF
          SYFT_TAG=${SYFT_TAG}
          SYFT_DIGEST=${syft_digest}
          SYFT_IMAGE=${SYFT_TAG}@${syft_digest}
          GRYPE_TAG=${GRYPE_TAG}
          GRYPE_DIGEST=${grype_digest}
          GRYPE_IMAGE=${GRYPE_TAG}@${grype_digest}
          COSIGN_TAG=${COSIGN_TAG}
          COSIGN_DIGEST=${cosign_digest}
          COSIGN_IMAGE=${COSIGN_TAG}@${cosign_digest}
          SBOM_FORMAT=spdx-json
          PROVENANCE_MODE=cosign-keyless-github-oidc
          EOF
          cmp /tmp/committed-locks/base-image.lock deploy/containers/base-image.lock
          cmp /tmp/committed-locks/supply-chain-tools.lock deploy/containers/supply-chain-tools.lock
          echo 'CONTAINER_TOOL_LOCK_REPRODUCIBILITY=PASS'

      - name: Regenerate hashed Python locks from frozen pyprojects
        run: |
          set -euo pipefail
          cat > /tmp/generate_inputs.py <<'PY'
          from __future__ import annotations
          import pathlib, re, tomllib
          projects = {}
          for path in sorted(pathlib.Path('.').glob('sitescore-*/pyproject.toml')):
              project = tomllib.loads(path.read_text()).get('project', {})
              if project.get('name'):
                  projects[project['name'].lower().replace('_','-')] = project
          def dep_name(spec):
              m = re.match(r'([A-Za-z0-9_.-]+)', spec.split(';', 1)[0].strip())
              if not m:
                  raise ValueError(spec)
              return m.group(1).lower().replace('_','-')
          def closure(start):
              seen = set(); external = {}
              def visit(name):
                  key = name.lower().replace('_','-')
                  if key in seen:
                      return
                  seen.add(key)
                  for spec in projects[key].get('dependencies', []):
                      child = dep_name(spec)
                      if child in projects:
                          visit(child)
                      else:
                          prior = external.get(child)
                          if prior is not None and prior != spec:
                              raise RuntimeError((child, prior, spec))
                          external[child] = spec
              visit(start)
              return [external[k] for k in sorted(external)]
          pathlib.Path('.faz7-api.in').write_text('\n'.join(closure('sitescore-api') + ['uvicorn==0.52.1']) + '\n')
          pathlib.Path('.faz7-commerce.in').write_text('\n'.join(closure('sitescore-commerce') + ['uvicorn==0.52.1']) + '\n')
          pathlib.Path('.faz7-build-tools.in').write_text('setuptools==80.9.0\nwheel==0.45.1\n')
          pathlib.Path('.faz7-test-tools.in').write_text('pytest==8.4.2\npypdf==6.14.2\n')
          PY
          python /tmp/generate_inputs.py
          docker run --rm -v "$PWD:/repo" -w /repo "${PYTHON_IMAGE_TAG}" sh -euxc '
            python -m pip install --disable-pip-version-check pip-tools==7.6.0
            pip-compile --generate-hashes --resolver=backtracking --strip-extras --output-file deploy/containers/api/requirements.lock .faz7-api.in
            pip-compile --generate-hashes --resolver=backtracking --strip-extras --output-file deploy/containers/commerce/requirements.lock .faz7-commerce.in
            pip-compile --generate-hashes --resolver=backtracking --strip-extras --output-file deploy/containers/build-tools.lock .faz7-build-tools.in
            pip-compile --generate-hashes --resolver=backtracking --strip-extras --output-file deploy/containers/test-tools.lock .faz7-test-tools.in
          '
          rm -f .faz7-api.in .faz7-commerce.in .faz7-build-tools.in .faz7-test-tools.in
          cmp /tmp/committed-locks/api-requirements.lock deploy/containers/api/requirements.lock
          cmp /tmp/committed-locks/commerce-requirements.lock deploy/containers/commerce/requirements.lock
          cmp /tmp/committed-locks/build-tools.lock deploy/containers/build-tools.lock
          cmp /tmp/committed-locks/test-tools.lock deploy/containers/test-tools.lock
          echo 'PYTHON_HASHED_LOCK_REPRODUCIBILITY=PASS'

      - name: Regenerate API native package lock from fixed Debian snapshot
        run: |
          set -euo pipefail
          image="$(awk -F= '$1=="PYTHON_IMAGE" {print $2}' deploy/containers/base-image.lock)"
          docker run --rm -e SNAPSHOT="${DEBIAN_SNAPSHOT}" "$image" sh -c '
            set -eu
            rm -f /etc/apt/sources.list.d/debian.sources
            cat > /etc/apt/sources.list <<EOF
          deb [check-valid-until=no] https://snapshot.debian.org/archive/debian/${SNAPSHOT}/ bookworm main
          deb [check-valid-until=no] https://snapshot.debian.org/archive/debian-security/${SNAPSHOT}/ bookworm-security main
          EOF
            apt-get -o Acquire::Check-Valid-Until=false update >/dev/null 2>&1
            for p in fontconfig fonts-dejavu-core libffi8 libjpeg62-turbo libopenjp2-7 libpango-1.0-0 libpangoft2-1.0-0 libharfbuzz-subset0 shared-mime-info; do
              v="$(apt-cache policy "$p" | awk "/Candidate:/ {print \$2}")"
              test -n "$v" && test "$v" != "(none)"
              printf "%s=%s\n" "$p" "$v"
            done
          ' > deploy/containers/api/apt-runtime.lock
          cmp /tmp/committed-locks/api-apt-runtime.lock deploy/containers/api/apt-runtime.lock
          echo 'APT_SNAPSHOT_LOCK_REPRODUCIBILITY=PASS'

      - name: Probe frozen n8n image and preserve SBOM vulnerability evidence
        run: |
          set -euo pipefail
          syft_image="$(awk -F= '$1=="SYFT_IMAGE" {print $2}' deploy/containers/supply-chain-tools.lock)"
          grype_image="$(awk -F= '$1=="GRYPE_IMAGE" {print $2}' deploy/containers/supply-chain-tools.lock)"
          docker pull --platform linux/amd64 "${N8N_IMAGE}"
          arch="$(docker image inspect "${N8N_IMAGE}" --format '{{.Architecture}}')"
          user="$(docker image inspect "${N8N_IMAGE}" --format '{{.Config.User}}')"
          printf 'N8N_ARCH=%s\nN8N_USER=%s\n' "$arch" "$user" | tee deploy/containers/artifacts/n8n-image-contract.txt
          test "$arch" = amd64
          test -n "$user" && test "$user" != 0 && test "$user" != root
          docker run --rm -v /var/run/docker.sock:/var/run/docker.sock "$syft_image" "${N8N_IMAGE}" -o spdx-json > deploy/containers/artifacts/n8n.spdx.json
          docker run --rm -v /var/run/docker.sock:/var/run/docker.sock "$grype_image" "${N8N_IMAGE}" -o json > deploy/containers/artifacts/n8n.grype.json
          python - <<'PY'
          import json, pathlib
          p = pathlib.Path('deploy/containers/artifacts')
          data = json.loads((p/'n8n.grype.json').read_text())
          blockers = []
          for match in data.get('matches', []):
              v = match.get('vulnerability', {})
              sev = str(v.get('severity', '')).upper()
              versions = (v.get('fix') or {}).get('versions') or []
              if sev == 'CRITICAL' or (sev == 'HIGH' and versions):
                  a = match.get('artifact', {})
                  blockers.append({'id': v.get('id'), 'severity': sev, 'package': a.get('name'), 'version': a.get('version'), 'fix_versions': versions})
          (p/'n8n-policy.json').write_text(json.dumps({'blocker_count': len(blockers), 'blockers': blockers}, indent=2, sort_keys=True) + '\n')
          print(f'N8N_POLICY_BLOCKERS={len(blockers)}')
          for b in blockers[:100]:
              print('N8N_BLOCKER', b)
          PY

      - name: Upload bootstrap evidence
        if: always()
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f
        with:
          name: faz7-7-1-bootstrap-evidence
          if-no-files-found: error
          retention-days: 14
          path: |
            deploy/containers/base-image.lock
            deploy/containers/supply-chain-tools.lock
            deploy/containers/build-tools.lock
            deploy/containers/test-tools.lock
            deploy/containers/api/requirements.lock
            deploy/containers/api/apt-runtime.lock
            deploy/containers/commerce/requirements.lock
            deploy/containers/artifacts/n8n-image-contract.txt
            deploy/containers/artifacts/n8n.spdx.json
            deploy/containers/artifacts/n8n.grype.json
            deploy/containers/artifacts/n8n-policy.json

      - name: Enforce frozen n8n vulnerability policy
        if: always()
        run: |
          set -euo pipefail
          test -s deploy/containers/artifacts/n8n-policy.json
          python - <<'PY'
          import json
          data = json.load(open('deploy/containers/artifacts/n8n-policy.json'))
          count = int(data['blocker_count'])
          print(f'N8N_POLICY_BLOCKERS={count}')
          if count:
              raise SystemExit(42)
          PY
