[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "sitescore-report"
version = "0.2.0"
description = "Canonical report facts and validated narrative authority for SiteScore AI"
requires-python = ">=3.11"
dependencies = [
    "sitescore-app==0.1.0",
    "sitescore-core==0.1.0",
    "openai==3.2.0",
    "pydantic==2.13.4",
]

[project.optional-dependencies]
dev = [
    "pytest==8.4.2",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
